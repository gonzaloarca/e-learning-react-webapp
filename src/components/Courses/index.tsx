import Layout from "../Layout";

const Courses = () => {
    return (
        <Layout>
            <div>
                <h1>Courses</h1>
            </div>
        </Layout>
    );
};

export default Courses;