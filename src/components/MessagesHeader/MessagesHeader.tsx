import { Button } from "antd";

const MessagesHeader = () => {
    return (
        <div>
            <h1>Messages</h1>
            <Button>
                Send New Message
            </Button>
        </div>
    );
};

export default MessagesHeader;